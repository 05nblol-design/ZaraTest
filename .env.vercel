# Variáveis de Ambiente para Deploy no Vercel
# Copie este arquivo e configure as variáveis no dashboard do Vercel

# MongoDB Atlas Connection String
# Obtenha no MongoDB Atlas Dashboard > Connect > Connect your application
MONGODB_URI=mongodb+srv://05:44092639@cluster0.hvggzox.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# JWT Secret Key
# Gere uma chave segura usando: openssl rand -base64 32
# Ou use um gerador online de chaves JWT
JWT_SECRET=sua_chave_jwt_super_secreta_aqui

# Configurações do Servidor
NODE_ENV=production
PORT=3000

# Configurações de CORS (opcional)
CORS_ORIGIN=https://seu-dominio-vercel.vercel.app

# Configurações de Rate Limiting (opcional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Configurações de Upload (opcional)
MAX_FILE_SIZE=5242880
UPLOAD_PATH=/tmp

# Configurações de Log (opcional)
LOG_LEVEL=info
LOG_FILE_ENABLED=false